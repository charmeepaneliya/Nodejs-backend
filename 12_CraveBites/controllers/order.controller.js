import HttpError from "../middleware/HttpError.js";
import order from "../models/order.model.js";

const add = async(req,res,next)=>{
    try {
        const {restaurant,items,totalAmount,status,address,phone}=req,body;

        
    } catch (error) {
        
    }
}